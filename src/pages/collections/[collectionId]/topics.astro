---
import AppLayout from '@/layouts/AppLayout.astro';
import { CollectionTopicsClient } from '@/components/collection-topics/CollectionTopicsClient';

if (!Astro.locals.auth?.isAuthenticated) {
  return Astro.redirect('/login');
}

const collectionId = Astro.params.collectionId;
if (!collectionId) {
  return Astro.redirect('/collections');
}
---

<AppLayout title="Tematy — 10x Facts">
  <CollectionTopicsClient client:load collectionId={collectionId} />
</AppLayout>

